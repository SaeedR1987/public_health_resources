% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_quality_tests.R
\name{quality_test_ttest_summary_rowwise}
\alias{quality_test_ttest_summary_rowwise}
\title{One-Sample T-Test from Actual Data (Row-wise Across Columns)}
\usage{
quality_test_ttest_summary_rowwise(
  survey_design,
  variables,
  expected_mean = 0,
  alternative = "two.sided",
  pval_colname = "p_value"
)
}
\arguments{
\item{survey_design}{A srvyr survey design object (e.g., created with \code{srvyr::as_survey_design()})}

\item{variables}{Character vector of column names whose values are used
row-wise for the t-test. At least 2 columns must be provided so that a
standard deviation can be estimated.}

\item{expected_mean}{Numeric scalar. Hypothesised population mean under the
null hypothesis (default: 0)}

\item{alternative}{Character scalar. Alternative hypothesis: \code{"two.sided"},
\code{"greater"}, or \code{"less"} (default: \code{"two.sided"})}

\item{pval_colname}{Character scalar. Name for the new p-value column
(default: \code{"p_value"})}
}
\value{
Data frame with an additional column containing the row-wise
  t-test p-values (rounded to 3 decimal places); rows with fewer than 2
  non-missing values receive \code{NA}
}
\description{
Perform a row-wise one-sample t-test on the actual data values. For each
row, the values across the columns specified in \code{variables} are
extracted and used to compute a one-sample t-test against
\code{expected_mean}. A p-value column is appended to the data frame.
}
