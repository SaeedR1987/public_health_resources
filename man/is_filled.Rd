% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{is_filled}
\alias{is_filled}
\title{Check if an object is filled (not NULL, not empty, not just "")}
\usage{
is_filled(x)
}
\arguments{
\item{x}{An object to check (usually a character string or vector).}
}
\value{
TRUE if the object is non-null, non-empty, and not all blank strings.
}
\description{
Check if an object is filled (not NULL, not empty, not just "")
}
\examples{
is_filled("abc")          # TRUE
is_filled(c("a", "b"))    # TRUE
is_filled(NULL)           # FALSE
is_filled(character(0))   # FALSE
is_filled("")             # FALSE
is_filled(c("a", ""))     # TRUE (since at least one is non-empty)
}
