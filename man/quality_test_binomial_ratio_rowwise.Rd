% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_quality_tests.R
\name{quality_test_binomial_ratio_rowwise}
\alias{quality_test_binomial_ratio_rowwise}
\title{Binomial Ratio Test (Row-wise)}
\usage{
quality_test_binomial_ratio_rowwise(
  survey_design,
  variables,
  expected_ratio = 0.5,
  alternative = "two.sided",
  pval_colname = "p_value"
)
}
\arguments{
\item{survey_design}{A srvyr survey design object (e.g., created with \code{srvyr::as_survey_design()})}

\item{variables}{Character vector of exactly 2 column names. The first
element is the column containing the number of successes (non-negative
integer) and the second is the column containing the total number of
trials (positive integer \eqn{\geq} success count).}

\item{expected_ratio}{Numeric scalar. Expected proportion under the null
hypothesis (default: 0.5; must be strictly between 0 and 1)}

\item{alternative}{Character scalar. Alternative hypothesis: \code{"two.sided"},
\code{"greater"}, or \code{"less"} (default: \code{"two.sided"})}

\item{pval_colname}{Character scalar. Name for the new p-value column
(default: \code{"p_value"})}
}
\value{
Data frame with an additional column containing the row-wise
  binomial test p-values (rounded to 5 decimal places); rows with missing
  or invalid values receive \code{NA}
}
\description{
Perform a row-wise exact binomial proportion test comparing the observed
success rate against an expected ratio, appending a p-value column to the
data frame.
}
