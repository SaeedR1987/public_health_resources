% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_quality_analyisis_outputs.R
\name{plot_stacked_bar_multiple_vars}
\alias{plot_stacked_bar_multiple_vars}
\title{Plot 100\% Stacked Bar Chart for Multiple Variables}
\usage{
plot_stacked_bar_multiple_vars(
  survey_design,
  category_vars,
  category_labels = NULL,
  grouping = NULL,
  show_overall = FALSE,
  overall_label = "Overall",
  weighted = FALSE,
  weights_col = NULL,
  flip_coordinates = FALSE,
  color_palette = "reach1",
  separate_legends = FALSE,
  title_name = NULL,
  variable_label = NULL,
  grouping_label = NULL,
  subtitle = NULL,
  x_label = NULL,
  y_label = "Percentage",
  legend_label = NULL,
  show_labels = FALSE,
  legend_position = "bottom",
  bar_spacing = 0.5,
  group_spacing = 0.1
)
}
\arguments{
\item{survey_design}{A srvyr survey design object (e.g., created with \code{srvyr::as_survey_design()})}

\item{category_vars}{Inputs a character vector specifying the column names for the categorical variables to plot (required, minimum 2).}

\item{category_labels}{Inputs an optional character vector of custom labels for each category variable.
If NULL, uses category_vars as labels. Must be same length as category_vars if provided.}

\item{grouping}{Inputs an optional character value specifying the column name for grouping variables within each bar.
If NULL, each variable shows overall distribution. If provided, shows grouped bars within each variable.}

\item{show_overall}{Logical. When grouping is provided, if TRUE, adds an "Overall" bar (leftmost) for each variable
showing the overall distribution across all groups. Default: FALSE. Only applies when grouping is not NULL.}

\item{overall_label}{Character. Label for the overall bar when show_overall = TRUE. Default: "Overall".}

\item{weighted}{Logical indicating whether to apply survey weights. Default: FALSE.}

\item{weights_col}{Character value specifying the column name containing survey weights.
Required if weighted = TRUE. Default: NULL.}

\item{flip_coordinates}{Logical. If TRUE, creates horizontal bars (flipped coordinates).
By default: FALSE (vertical bars).}

\item{color_palette}{Inputs a character value or vector specifying the color palette(s) to use.
Can be a single palette applied to all variables, or a vector of palettes (one per variable).
Options: "reach1", "reach2", "reach3", "reach4", "traffic_light", "default". Default is "reach1".}

\item{separate_legends}{Logical. If TRUE, creates truly separate legend for each variable using ggnewscale.
If FALSE, uses single combined legend. Default: FALSE. Requires ggnewscale package when TRUE.}

\item{title_name}{Inputs an optional character value for the title of the plot.}

\item{subtitle}{Inputs an optional character value for the subtitle of the plot.
If NULL, automatically displays n (number of records). Custom subtitle will be appended to n display.}

\item{x_label}{Inputs an optional character value for the x-axis label.}

\item{y_label}{Inputs an optional character value for the y-axis label (default: "Percentage").}

\item{legend_label}{Inputs an optional character value or vector for the legend title(s).
If NULL, uses variable names. Can be a vector (one per variable) if separate_legends = TRUE.}

\item{show_labels}{Inputs a logical value indicating whether to show percentage labels on bars (default: FALSE).}

\item{legend_position}{Position of the legend. By default: "bottom". Options: "bottom", "top", "left", "right", "none".}

\item{bar_spacing}{Numeric value for spacing between variable groups (default: 0.5).
Higher values create more space between different variables.}

\item{group_spacing}{Numeric value for spacing between bars within a variable when using grouping (default: 0.1).
Creates visual separation between different variables' grouped bars.}
}
\value{
Returns a ggplot2 object showing the 100\% stacked bar chart with multiple variables.
}
\description{
Creates a 100\% stacked bar chart showing multiple categorical variables in the same plot.
Each variable is represented as a separate bar, optionally grouped together.
The plot shows the relative proportions of each category as a percentage of the total.
Supports survey weights for proper representation of weighted surveys.
Allows different color palettes and separate legends for each variable.
}
