% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_quality_analyisis_outputs.R
\name{plot_age_pyramid}
\alias{plot_age_pyramid}
\title{Plot Age Pyramid}
\usage{
plot_age_pyramid(
  survey_design,
  sex_col,
  age_years_col,
  age_grouping = FALSE,
  age_group_col = NULL,
  sex_male_val = NULL,
  sex_female_val = NULL,
  sex_male_lab = NULL,
  sex_female_lab = NULL,
  weights_col = NULL,
  weighted_result = FALSE,
  proportional = TRUE,
  color_palette = "sex",
  title_name = NULL,
  variable_label = NULL,
  subtitle = NULL,
  x_lab = NULL,
  y_lab = NULL,
  legend_position = "bottom"
)
}
\arguments{
\item{survey_design}{A srvyr survey design object (e.g., created with \code{srvyr::as_survey_design()})}

\item{sex_col}{(Required) The variable name in the Data indicating the sex of the individual.}

\item{age_years_col}{(Required) The variable name in the Data indicating the age of the individual.}

\item{age_grouping}{If TRUE, user is using age_grouping in data with variable name
specified in age_group_col. If FALSE (default), age_years_col will be grouped as follows:
0-4/5-9/10-14/15-19/20-24/25-29/30-34/35-39/40-44/
45-49/50-54/55-59/60-64/65-69/70-74/75-79/80-84/85+}

\item{age_group_col}{The variable name for pre-grouped age data.
By default: NULL. Only used when age_grouping = TRUE.}

\item{sex_male_val}{The value in the sex_col that represents males. By default: NULL (falls back to "1")}

\item{sex_female_val}{The value in the sex_col that represents females. By default: NULL (falls back to "2")}

\item{sex_male_lab}{The label to display for males in the legend. By default: NULL (falls back to "Male")}

\item{sex_female_lab}{The label to display for females in the legend. By default: NULL (falls back to "Female")}

\item{weights_col}{The variable name for survey weights. By default: NULL (no weights applied).
If provided, must be numeric and non-NA values will be used for weighting.}

\item{weighted_result}{Logical. If TRUE and weights_col is provided, uses weighted counts.
If FALSE or weights_col is NULL, uses unweighted counts. By default: FALSE.}

\item{proportional}{Logical. If TRUE (default), shows proportions. If FALSE, shows counts.}

\item{color_palette}{Inputs an optional character value specifying the color palette to use.
Options: "sex" (default for male/female comparison), "reach1", "reach2", "reach3", "reach4", "traffic_light", "default".}

\item{title_name}{Title of the plot. By default: NULL.}

\item{variable_label}{Optional character string. Human-readable label for the variable, used to
auto-generate the plot title when title_name is NULL. Default: NULL.}

\item{subtitle}{Inputs an optional character value for the subtitle of the plot.
If NULL, automatically displays n by sex (e.g., "Male (n=X); Female (n=Y)"). Custom subtitle will be appended to n display.}

\item{x_lab}{Label for the x-axis. By default: NULL.
If NULL, uses automatic default: "Proportion of Population" or "Count" based on proportional parameter.}

\item{y_lab}{Label for the y-axis. By default: NULL (falls back to "Age Group").}

\item{legend_position}{Position of the legend. By default: "bottom". Options: "bottom", "top", "left", "right", "none".}
}
\value{
An Age Pyramid plot
}
\description{
Plot Age Pyramid
}
\examples{
\dontrun{
  plot_age_pyramid(survey_design = hh_roster, sex_col = "sex", age_years_col = "age_years")
  plot_age_pyramid(survey_design = hh_roster, sex_col = "gender", age_years_col = "age",
                   sex_male_val = "m", sex_female_val = "f")
  plot_age_pyramid(survey_design = hh_roster, sex_col = "sex", age_years_col = "age_years",
                   weights_col = "survey_weight", weighted_result = TRUE)
}
}
