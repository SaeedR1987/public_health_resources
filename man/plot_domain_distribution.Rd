% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_quality_analyisis_outputs.R
\name{plot_domain_distribution}
\alias{plot_domain_distribution}
\title{Plot Select Multiple Across Domains}
\usage{
plot_domain_distribution(
  df,
  domain_list,
  show_percentage = FALSE,
  flip_coordinates = TRUE,
  color_palette = "reach1",
  title_name = NULL,
  subtitle = NULL,
  x_lab = NULL,
  y_lab = NULL,
  legend_position = "bottom",
  weighted = FALSE,
  weights_col = NULL
)
}
\arguments{
\item{df}{Inputs a dataframe.}

\item{domain_list}{A named list where each element represents a domain. Each domain element
should be a list with:
  - 'responses': character vector of column names for that domain
  - 'label': (optional) display label for the domain
  - 'response_labels': (optional) named vector mapping column names to display labels
Example: list(
  availability = list(
    responses = c("barrier_no_staff", "barrier_no_medicine"),
    label = "Availability Issues",
    response_labels = c("barrier_no_staff" = "No Staff", "barrier_no_medicine" = "No Medicine")
  )
)}

\item{show_percentage}{Logical. If TRUE, shows percentages instead of counts.
By default: FALSE. Note: For select multiple questions, percentages may sum to >100%.}

\item{flip_coordinates}{Logical. If TRUE, creates horizontal bars (flipped coordinates).
By default: TRUE.}

\item{color_palette}{Color palette to use. By default: "reach1"}

\item{title_name}{Title of the plot. By default: NULL}

\item{subtitle}{Subtitle of the plot. By default: NULL}

\item{x_lab}{Label for x-axis. By default: NULL (auto-generated based on show_percentage and flip_coordinates)}

\item{y_lab}{Label for y-axis. By default: NULL (auto-generated based on flip_coordinates)}

\item{legend_position}{Position of the legend. By default: "bottom". Options: "bottom", "top", "left", "right", "none".}

\item{weighted}{Logical. If TRUE, applies survey weights to produce weighted results. By default: FALSE.}

\item{weights_col}{Character string. Name of the column containing survey weights. Required when weighted = TRUE.}
}
\value{
Returns a ggplot2 object.

Returns a ggplot2 object.
}
\description{
This functions plots a count distribution of responses for a select multiple question
and shows their responses distributed against some domain categorization of those
response. E.g A health barriers question with several response options, however those
response options can be grouped by barriers related to availability, accessibility, or
quality of services. Each response option should only belong to one domain category.
}
\examples{
\dontrun{
  domain_list <- list(
    accessibility = list(
      responses = c("barrier.distance", "barrier.costtransport"),
      label = "Accessibility Barriers",
      response_labels = c("barrier.distance" = "Too Far",
                         "barrier.costtransport" = "Transport Cost")
    ),
    quality = list(
      responses = c("barrier.rudestaff"),
      label = "Quality Barriers",
      response_labels = c("barrier.rudestaff" = "Rude Staff")
    )
  )
  plot_domain_distribution(df = data, domain_list = domain_list)
}
\dontrun{
  domain_list <- list(
    accessibility = list(
      responses = c("barrier.distance", "barrier.costtransport"),
      label = "Accessibility Barriers",
      response_labels = c("barrier.distance" = "Too Far",
                         "barrier.costtransport" = "Transport Cost")
    )
  )
  # Horizontal bars (default)
  plot_domain_distribution(df = data, domain_list = domain_list)

  # Vertical bars
  plot_domain_distribution(df = data, domain_list = domain_list, flip_coordinates = FALSE,
                              legend_position = "bottom")

  # Show percentages
  plot_domain_distribution(df = data, domain_list = domain_list, show_percentage = TRUE)
}
}
