% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data_individual_nutrition.R
\name{add_mfaz}
\alias{add_mfaz}
\title{Add MFA-Z-Based Classifications and Flags}
\usage{
add_mfaz(
  .dataset,
  nut_muac_cm_col,
  edema_confirm_col,
  child_age_months_col,
  child_sex_col,
  male_sex_val,
  female_sex_val,
  edema_confirm_val,
  grouping = NULL
)
}
\arguments{
\item{.dataset}{A data frame or tibble containing the required columns.}

\item{nut_muac_cm_col}{Column name (character) of the MUAC measurements (in cm).}

\item{edema_confirm_col}{Column name (character) confirming the presence of bilateral pitting edema.}

\item{child_age_months_col}{Column name (character) of the child's age in months.}

\item{child_sex_col}{Column name (character) of the child's sex.}

\item{male_sex_val}{Character value representing "male" in the `child_sex_col` column.}

\item{female_sex_val}{Character value representing "female" in the `child_sex_col` column.}

\item{edema_confirm_val}{Character value representing confirmed edema in the `edema_confirm_col` column.}

\item{grouping}{Optional character vector of column names for grouping mean and SD calculations for `flag_sd_mfaz`.}
}
\value{
A data frame with the following new columns:
- **mfaz**
- **severe_mfaz**
- **moderate_mfaz**
- **global_mfaz**
- **flag_sd_mfaz**
}
\description{
Calculates severe, moderate, and global MFA-Z classifications, as well as flags for extreme MFA-Z values.
}
\details{
The function adds the following columns to the dataset:
- **mfaz**: The calculated MFA-Z score using the `zscorer` package.
- **severe_mfaz**: Severe malnutrition (1 = severe, 0 = not severe).
- **moderate_mfaz**: Moderate malnutrition (1 = moderate, 0 = not moderate).
- **global_mfaz**: Global malnutrition (1 = global, 0 = not global).
- **flag_sd_mfaz**: 1 if the MFA-Z value is less than `mean - 4 * SD` or greater than `mean + 3 * SD`, 0 otherwise.

The function avoids overwriting any existing `sex` column in the dataset by creating a temporary internal column (`temp_sex_for_zscorer`) for sex recoding (1 for male, 2 for female).
This column is used for calculations with `zscorer` and is removed from the dataset after processing.

If a `grouping` argument is provided, the mean and standard deviation of MFA-Z values are calculated within these groups for determining the flag for extreme values (`flag_sd_mfaz`).
}
\examples{
# Example dataset
df <- data.frame(
  nut_muac_cm = c(12.5, 10.5, 14.0),
  child_sex = c("m", "f", "f"),
  child_age_months = c(24, 30, 18),
  nut_edema_confirm = c("yes", NA, "no")
)

# Add MFA-Z classifications and flags
df_result <- add_mfaz(
  .dataset = df,
  nut_muac_cm_col = "nut_muac_cm",
  edema_confirm_col = "nut_edema_confirm",
  child_age_months_col = "child_age_months",
  child_sex_col = "child_sex",
  male_sex_val = "m",
  female_sex_val = "f",
  edema_confirm_val = "yes"
)

print(df_result)

}
